import os
from config import Config

class Data:
        pass
    
        #* API data
        # API_LOGIN = Config.config[os.environ['STAGE']]['API_LOGIN']
        # API_PASSWORD = Config.config[os.environ['STAGE']]['API_PASSWORD']

        # #* Account data
        # ACCOUNT_LOGIN = Config.config[os.environ['STAGE']]['LOGIN']
        # ACCOUNT_PASSWORD = Config.config[os.environ['STAGE']]['PASSWORD']
        # CHANGED_PASSWORD = Config.config[os.environ['STAGE']]['NEW_PASSWORD']

        # #* Credit card data
        # CVV = Config.config[os.environ['STAGE']]['CVV']
        # CARD_NUMBER = Config.config[os.environ['STAGE']]['CARD_NUMBER']

        # #* Cookies data 
        # OLD_COOKIES_NAME = Config.config[os.environ['STAGE']]['OLD_COOKIES_NAME']
        # NEW_COOKIES_NAME = Config.config[os.environ['STAGE']]['NEW_COOKIES_NAME']
        
        # #* Letters data
        # REGISTRATION_LETTER = Config.config[os.environ['STAGE']]['REGISTRATION_LETTER']
        # ACCOUNT_CANCELED_LETTER = Config.config[os.environ['STAGE']]['CANCELED_LATTER']